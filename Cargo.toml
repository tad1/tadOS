[package]
name = "tad_os"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
aarch64-cpu = "9.4.0"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"